# SPEC: <change 标题>

- **Change ID**: <id>
- **关联**: `@.gantry/specs/<id>/PROPOSAL.md`、`@.gantry/specs/CONTEXT.md`

---

## 用户故事

- **US-1**：作为 <角色>，我想 <动作>，以便 <价值>。

## 验收准则（AC）

### AC-1 · <短标题>

- **Given** <初始状态>
- **When** <用户动作>
- **Then** <可观察结果>
- **验证方式**: `<命令或手动 UAT 编号>`

## 关键用户路径（跨服务时填 · 单服务写「无跨服务路径」）

> 一条 Journey = 由多个 AC 串成的、跨多个服务的端到端用户旅程。它是 INTEGRATION 阶段端到端验收与断点归属的唯一成功判据来源。

### J1 · <路径短标题>

- **路径 AC 串**: AC-3（下单）→ AC-5（锁库存）→ AC-7（支付）→ AC-9（通知）
- **端到端成功判据**: <用户可观察的最终成功状态，例如「用户看到支付成功 且 订单=paid 且 库存已扣」>
- **涉及服务（初判）**: <order / stock / pay / notify>
- **失败回滚要求**: <任一环失败时，已扣资源必须回滚，不允许部分成功悬挂>

## 范围切分

### v1（本次必做）

- <列表>

### v2（下一轮考虑，不本次）

- <至少 1 条>

### out（永远不做）

- <至少 1 条>

## 非功能性需求

- **性能**: <例如 首屏 LCP ≤ 2.5s；如无写"无">
- **可访问性**: <例如 WCAG 2.1 AA；如无写"无">
- **安全**: <如无写"无">
- **兼容性**: <浏览器/系统版本范围；如无写"无">
- **可观测性**: <日志/埋点要求；如无写"无">

## 多端差异（如适用）

- **Web**: <差异>
- **Mobile**: <差异>
- **Server/API**: <差异>

## 依赖与假设

- <依赖的外部服务、库、内部模块>
- <已假设的前提条件>

## Open Items

- [ ] `<blocking|non-blocking>` <待确认问题>

## CONTEXT 候选 patch

> REQUIREMENT 阶段只在这里记录候选。只有通过 CONTEXT 质量门、与 LESSONS 无冲突、且影响范围不是"本 change"的条目，才允许同步写入 `.gantry/specs/CONTEXT.md`。

| 类型 | 内容 | 来源 | 影响范围 | 冲突检查 | 处理 |
|---|---|---|---|---|---|
| term / decision / default | <内容> | <file:line / 用户确认> | <全项目 / 模块 / 本 change> | <无冲突 / 与 L-NNN 冲突 / 与 CONTEXT:X 冲突> | <已写入 CONTEXT / 待用户确认 / 仅保留在本 SPEC> |

---

> AC 是 TEST 阶段派生用例的唯一来源，禁止在 TEST 阶段引入新 AC。
